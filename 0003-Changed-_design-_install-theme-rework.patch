From 114ba6e5f8dca0b5e9d22a63602c0af1b9f38b7b Mon Sep 17 00:00:00 2001
From: David Perchermeier <mail@dape.io>
Date: Wed, 26 Aug 2026 13:28:50 +0200
Subject: [PATCH 3/3] Changed(_design,_install): theme rework

---
 CHANGELOG.md                            |  31 +++++
 _design/Design.php                      | 145 +++++++++++++++++++++---
 _design/assets/design.js                |  13 ++-
 _design/templates/preview-example.tpl   | 108 +++++-------------
 _install/Install.php                    |  75 +++++++++++-
 _install/assets/design.js               |  11 +-
 _install/library/footer/v1/template.tpl |  24 ++--
 _install/library/footer/v2/template.tpl |  78 +++++++------
 _install/library/footer/v3/template.tpl |  40 ++++---
 _install/library/footer/v6/template.tpl |  36 +++---
 _install/library/header/v1/style.css    |   2 +-
 _install/library/header/v1/template.tpl |  20 ++--
 _install/library/header/v3/template.tpl |  20 ++--
 _install/library/header/v4/template.tpl |  20 ++--
 _nino/Nino.admin.js                     |  29 ++++-
 tests/design-js-smoke.js                |  16 ++-
 tests/design-smoke.php                  |   8 +-
 tests/install-design-js-smoke.js        |  11 +-
 tests/install-smoke.php                 |  13 +++
 19 files changed, 469 insertions(+), 231 deletions(-)

diff --git a/CHANGELOG.md b/CHANGELOG.md
index fe89ca0..d9ad9d8 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -101,6 +101,37 @@ All notable changes to Nino are documented in this file.
   missing it. Each option's text is the position's name and its value the number
   that gets stored, so neither side translates the other's vocabulary.
 
+- The Design preview shows the project's own header and footer, its own
+  pictures, and the whole page rather than the first screen of it.
+
+  The bar and the footer used to be drawn in the example template out of
+  framework classes. That is a bar and a footer no site has - and on **Rail**,
+  whose whole idea is a vertical navigation rail instead of a top bar, it was
+  not a simplification but the wrong picture: the operator was judging colours
+  on a layout the theme does not use. `/_install` already knew how to prepare a
+  frame unit for a preview, so `Preview` asks rather than rebuilding
+  (`Themes::frameUnit()`, `Themes::previewFrameKey()`); the installed frame
+  decides, or - before the Header and Footer steps have run - the one the
+  active theme declares.
+
+  The pictures are the demo images the catalogue page ships, named in the
+  markup the way a page names them and carried into the document by
+  `Preview::images()`, since a srcdoc frame has an opaque origin and fetches
+  nothing. Grey rectangles previewed a design against no photograph at all,
+  which is not what any of these themes are for.
+
+  `Nino.adminUi.scaleFrame()` takes a layout height beside the layout width.
+  With both fixed the scale is whichever of the two fits, so the whole page is
+  on screen at a stable layout and what changes with the panel is how small it
+  is drawn - not how much of it there is. It also stops the cover's own `vh`
+  lengths changing with the operator's window. 2400x2800 fits all ten themes,
+  the tallest with 34px to spare.
+
+  And more of the brand is on the page: the services band is the tint ground -
+  the one surface the brand hue actually reaches, which had no band of its own -
+  and the cards carry a filled `--color-primary` badge, which is that role at
+  text size rather than at button size.
+
 - The example the Design pane previews against is a page now, not a strip of
   specimens: a bar, a hero, three cards, a split band with a photograph and the
   four status surfaces, and a footer. What an operator is deciding is how a
diff --git a/_design/Design.php b/_design/Design.php
index 4c9cac0..cd8697d 100644
--- a/_design/Design.php
+++ b/_design/Design.php
@@ -1988,6 +1988,17 @@ namespace Nino\Design {
 
 		private const BUNDLE_KEY = '/.cache/style.css';
 
+		// A ceiling per picture. The demo images are half a kilobyte of svg,
+		// so this is generous - it is here so a project that dropped a 12mb
+		// photograph into the path cannot turn one preview request into one
+		// too large to send
+		private const int IMAGE_LIMIT = 262144;
+
+		private const array IMAGE_TYPES = [
+			'svg' => 'image/svg+xml', 'png' => 'image/png',
+			'jpg' => 'image/jpeg', 'jpeg' => 'image/jpeg', 'webp' => 'image/webp',
+		];
+
 		/**
 		 *	One complete html document showing what these settings produce.
 		 *
@@ -2005,14 +2016,117 @@ namespace Nino\Design {
 				return '';
 
 			$mode = $mode === 'dark' ? 'dark' : 'light';
+			$frames = self::frames( $appData );
+
+			$css = self::css( $appData, $settings );
+
+			foreach( $frames as $frame )
+				$css .= "\n". $frame['css'];
+
+			$markup = ( $frames['header']['markup'] ?? '' )
+				. self::images( $appData, $markup )
+				. ( $frames['footer']['markup'] ?? '' );
 
 			return '<!doctype html><html lang="en" data-nino-mode="'. $mode. '"><head><meta charset="utf-8">'
 				. '<meta name="viewport" content="width=device-width, initial-scale=1">'
-				. '<style>'. self::css( $appData, $settings ). '</style>'
-				. '<style>'. self::scaffold(). '</style>'
+				. '<style>'. $css. '</style>'
+				. '<style>'. self::scaffold( isset( $frames['header'] ) ). '</style>'
 				. '</head><body>'. $markup. '</body></html>';
 		}
 
+		/**
+		 *	The header and footer this project actually has, ready to paste
+		 *	around the example.
+		 *
+		 *	The pane used to draw a bar and a footer of its own out of framework
+		 *	classes. That is a bar and a footer no site has, and on a look whose
+		 *	whole idea is a vertical rail rather than a top bar it was not a
+		 *	simplification but the wrong picture - the operator was judging the
+		 *	colours of a layout the theme does not use. The real units are right
+		 *	there in the library, and /_install already knows how to prepare one
+		 *	for a preview, so this asks rather than rebuilding.
+		 *
+		 *	A delivery may ship without /_install, and then there are no frames
+		 *	to have: the example stands on its own, as it did before.
+		 *
+		 *	@param		array 		&$appData			(reference) Array with current app data
+		 *
+		 *	@return 	array									kind => { frame, markup, css }
+		 */
+		private static function frames( array &$appData ): array {
+
+			if( class_exists( "\\Nino\\Install\\Themes" ) === false
+				|| method_exists( "\\Nino\\Install\\Themes", 'frameUnit' ) === false )
+				return [];
+
+			$frames = [];
+
+			foreach( [ 'header', 'footer' ] as $kind ) {
+
+				$key = \Nino\Install\Themes::previewFrameKey( $appData, $kind );
+
+				if( $key === null )
+					continue;
+
+				$unit = \Nino\Install\Themes::frameUnit( $kind, $key );
+
+				if( $unit !== null )
+					$frames[$kind] = $unit;
+			}
+
+			return $frames;
+		}
+
+		/**
+		 *	The pictures the example points at, carried into it.
+		 *
+		 *	The markup names them the way a page does - a path under the public
+		 *	directory - because that is the whole rule this example lives by.
+		 *	But a srcdoc frame has an opaque origin and fetches nothing, so the
+		 *	file travels as data or not at all. The project's own copy first,
+		 *	then the library's, so a preview has pictures before the page unit
+		 *	that ships them has been installed.
+		 *
+		 *	Only files under the public directory, and only images: the src is
+		 *	written in a template this project owns, but a template is still not
+		 *	a reason to read an arbitrary path.
+		 *
+		 *	@param		array 		&$appData			(reference) Array with current app data
+		 *	@param		string		$markup				The example
+		 *
+		 *	@return 	string
+		 */
+		private static function images( array &$appData, string $markup ): string {
+
+			return (string) preg_replace_callback( '~\[\[/nino/public\]\](/[a-z0-9./_-]+\.(?:svg|png|jpe?g|webp))~i', static function( array $match ) use ( &$appData ): string {
+
+				$relative = $match[1];
+
+				if( str_contains( $relative, '..' ) === true )
+					return '';
+
+				$candidates = [ (string) \Nino\Filesystem::path( $appData, $relative ) ];
+
+				// The same file as the demo page unit ships, read from the
+				// library when the project has not installed that unit
+				$candidates[] = __DIR__. '/../_install/library/pages/.demo-catalogue'. $relative;
+
+				foreach( $candidates as $path )
+					if( is_file( $path ) === true && filesize( $path ) <= self::IMAGE_LIMIT ) {
+
+						$data = (string) @file_get_contents( $path );
+
+						if( $data !== '' )
+							return 'data:'. self::IMAGE_TYPES[strtolower( pathinfo( $path, PATHINFO_EXTENSION ) )]
+								. ';base64,'. base64_encode( $data );
+					}
+
+				// Better an empty frame than a broken-image icon, which reads
+				// as the design's fault rather than as a missing file
+				return '';
+			}, $markup );
+		}
+
 		/**
 		 *	Everything a real page would load, in the order a real page loads
 		 *	it: the framework, the values being previewed, then whatever the
@@ -2096,22 +2210,19 @@ namespace Nino\Design {
 		 *
 		 *	@return 	string
 		 */
-		private static function scaffold(): string {
-
-			return 'body{margin:0}'
-				/*	Nino.css puts a bare <header> at position:fixed, and every
-					header unit in the library pays for that with a padding on
-					main - the frames differ in what they draw, never in that.
-					The example carries no frame, so the one rule they all share
-					is supplied here rather than written into the markup, where
-					it would be a length this preview had invented.	*/
-				. 'body main{padding-top:clamp(5rem,var(--space-6),8rem)}'
-				/*	...and the other one: every frame lays its nav out in a row
-					on a wide screen and pads the links to SC 2.5.8's 24px. The
-					framework owns the list reset and the burger, never the row -
-					that is the frame's decision, and the example has no frame.	*/
+		private static function scaffold( bool $framed = false ): string {
+
+			/*	Two rules a real page gets from its header unit rather than from
+				the framework: the padding that keeps a fixed bar off the first
+				heading, and the row the nav links sit in. When the example is
+				shown inside the project's actual frame that unit supplies both,
+				and repeating them here would override what it decided - so they
+				are only here for a delivery that has no frames at all.	*/
+			$frame = $framed === true ? '' : 'body main{padding-top:clamp(5rem,var(--space-6),8rem)}'
 				. 'header .nino-nav-content ul{display:flex;gap:var(--space-2)}'
-				. 'header .nino-nav-content li a{display:inline-block;padding:var(--space-1) 0}'
+				. 'header .nino-nav-content li a{display:inline-block;padding:var(--space-1) 0}';
+
+			return 'body{margin:0}'. $frame
 				// A face the iframe can actually resolve, since the theme's own
 				// were stripped with the @font-face rules that declared them
 				. ':root{'
diff --git a/_design/assets/design.js b/_design/assets/design.js
index a89e277..665764b 100644
--- a/_design/assets/design.js
+++ b/_design/assets/design.js
@@ -42,7 +42,16 @@
 		// which half of a design that always has both halves is on screen
 		_mode : 'light',
 		_rowsBound : false,
-		PREVIEW_WIDTH : 1600,
+		/*	The layout the example is rendered at, and then scaled into the
+			panel. A desktop's width, because a page never meets its own
+			layout limits in a box half a pane wide - and a height to go with
+			it, so the whole page is on screen rather than the first screen of
+			it. A page with a header, cards carrying photographs, a split band
+			and a real footer is about this tall at this width; giving it the
+			panel's height instead would cut it off, and the cover's own vh
+			lengths would change with the operator's window.	*/
+		PREVIEW_WIDTH : 2200,
+		PREVIEW_HEIGHT : 2000,
 		_refit : null,
 		_designChoices : {},
 		_designBound : false,
@@ -750,7 +759,7 @@
 			const port = dc.getElementById('theme-design-example-port');
 
 			if( frame !== null && port !== null )
-				Nino.design._refit = Nino.adminUi.scaleFrame( frame, port, Nino.design.PREVIEW_WIDTH );
+				Nino.design._refit = Nino.adminUi.scaleFrame( frame, port, Nino.design.PREVIEW_WIDTH, Nino.design.PREVIEW_HEIGHT );
 
 			Nino.adminUi.buttonRow( {
 				light : dc.getElementById('theme-design-mode-light'),
diff --git a/_design/templates/preview-example.tpl b/_design/templates/preview-example.tpl
index b02c60f..50e83d2 100644
--- a/_design/templates/preview-example.tpl
+++ b/_design/templates/preview-example.tpl
@@ -11,66 +11,37 @@
 	the same utilities, the same spacing classes. That is the whole point of it
 	- a mock with margins of its own would preview its own margins.
 
-	Five bands, each there for something the settings move:
+	What this file does *not* carry is the bar and the footer. Those are the
+	project's own header and footer units, prepared by /_install and pasted
+	around this markup by Preview::document(): a bar drawn here out of framework
+	classes is a bar no site has, and on a look built around a vertical rail
+	rather than a top bar it was not a simplification but the wrong picture.
+	Everything between them is this file.
 
-		the bar          the page ground and type at small sizes
-		the hero         the display end of the scale, and both brand buttons
+	Four bands, each there for something the settings move:
+
+		the cover        the display end of the scale over a photograph, and
+		                 all three button roles at once
 		three cards      the alternate surface, borders, corners, images
 		the split band   a second ground, body copy, and the four status
 		                 surfaces the brand knobs are not allowed to reach
-		the footer       the deepest ground, where most themes close a page
+		the closing band the second brand colour as a surface of its own
 
 	Sized to be taken in at once. The frame renders at a desktop's layout width
 	and is scaled into the panel, so the room is horizontal rather than
 	vertical, and the page is one screen of a site rather than all of one -
 	scrolling a preview to find the rest of it is scrolling to find out whether
-	a decision was right. What that costs is the band padding: the sections
-	take space-2 where a live page usually takes the section default of
-	space-4. Every other distance in here is the one a real page gets.
-
-	The header and footer are the framework's own. Nino.css styles a bare
-	<header> and the two footer bands without a frame unit, which is why they
-	can be here at all; what a real site's frame adds on top is the Header and
-	Footer dialogs' question, not this one's. The one rule every frame in the
-	library does carry - the padding that keeps a fixed bar off the first
-	heading - is supplied by Preview::scaffold() rather than written in here,
-	where it would be a length this preview had invented.
-
-	The photographs are data uris: a srcdoc frame has an opaque origin and can
-	fetch nothing. A grey placeholder is honest about being one, and a picture
-	is not a design token.	-->
+	a decision was right.
 
-<header>
-	<div class="nino-grid-row">
-		<div class="nino-grid-100">
-			<a href="#" class="nino-logo">
-				<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 260 100" width="260" height="100">
-				  <rect width="260" height="100" fill="#f8f9fa" rx="8"/>
-				  <g transform="translate(130, 50)" fill="#4a6baf">
-				    <rect x="-10" y="-40" width="10" height="80" rx="2"/>
-				    <rect x="-10" y="30" width="40" height="10" rx="2"/>
-				  </g>
-				</svg>
-			</a>
-			<nav class="nino-nav-content">
-				<ul>
-					<li><a href="#">Work</a></li>
-					<li><a href="#">Studio</a></li>
-					<li><a href="#">Journal</a></li>
-					<li><a href="#">Contact</a></li>
-				</ul>
-			</nav>
-		</div>
-	</div>
-</header>
-
-<!--	<main> because Nino.css gives it flex: 1 0 auto inside a body that is a
-	column at least a viewport tall - which is what puts the footer at the foot
-	of the frame instead of leaving the rest of it blank.	-->
+	The pictures are named the way a page names them, a path under the public
+	directory, and Preview::images() carries them into the document - a srcdoc
+	frame has an opaque origin and fetches nothing. They are the same demo
+	images the catalogue page uses, so the preview shows the project's own
+	pictures rather than a grey rectangle standing in for one.	-->
 <main>
 
-<section style="min-height:40vh" class="nino-atf nino-pt-1 nino-pb-2 nino-mt-0 nino-mb-0 nino-cover nino-cover--dim" data-cover-height="60">
-	<img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 640 380'%3E%3Crect width='640' height='380' fill='%23aeb3b9'/%3E%3Ccircle cx='492' cy='96' r='48' fill='%23c9cdd3'/%3E%3Cpath d='M0 380 196 148 372 380z' fill='%23979da5'/%3E%3Cpath d='M296 380 448 206 640 380z' fill='%238b9199'/%3E%3C/svg%3E" alt="">
+<section style="min-height:28vh" class="nino-atf nino-pt-1 nino-pb-2 nino-mt-0 nino-mb-0 nino-cover nino-cover--dim" data-cover-height="30">
+	<img src="[[/nino/public]]/images/.demo/demo-00.svg" alt="">
 	<div class="nino-cover-content">
 		<div class="nino-grid-row">
 			<div class="nino-grid-100 nino-grid-m-66 nino-pt-6 nino-text-left">
@@ -86,36 +57,36 @@
 	</div>
 </section>
 
-<section class="nino-section nino-mt-0 nino-mb-0 nino-pt-3 nino-pb-3">
+<section class="nino-section nino-section--tint nino-mt-0 nino-mb-0">
 	<div class="nino-grid-row">
 		<div class="nino-grid-100">
 			<h4 class="nino-section-title">Our Services</h4>
 			<p class="nino-section-subtitle">All the best for you</p>
 		</div>
 		<div class="nino-grid-33">
-			<article class="nino-article nino-article--alt nino-mb-3"><img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 480 140'%3E%3Crect width='480' height='140' fill='%23b8bcc2'/%3E%3Ccircle cx='378' cy='44' r='22' fill='%23cfd3d8'/%3E%3Cpath d='M0 140 152 62 272 140z' fill='%23a2a7ae'/%3E%3Cpath d='M212 140 332 78 480 140z' fill='%23949aa2'/%3E%3C/svg%3E" alt="" class="nino-article-img"><h3 class="nino-article-title">Consulting</h3><div class="nino-article-descr">In a conversation, we work out what you need.</div><a href="#">Learn more</a></article>
+			<article class="nino-article nino-article--alt nino-mb-3"><img src="[[/nino/public]]/images/.demo/demo-01.svg" alt="" class="nino-article-img nino-article-img--maxheight"><h3 class="nino-article-title">Consulting <span class="nino-badge nino-badge--primary">Popular</span></h3><div class="nino-article-descr">In a conversation, we work out what you need.</div><a href="#">Learn more</a></article>
 		</div>
 		<div class="nino-grid-33">
-			<article class="nino-article nino-article--alt nino-mb-3"><img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 480 140'%3E%3Crect width='480' height='140' fill='%23b0b5bb'/%3E%3Crect x='58' y='28' width='142' height='84' fill='%23c8ccd2'/%3E%3Crect x='240' y='52' width='170' height='60' fill='%239aa0a8'/%3E%3C/svg%3E" alt="" class="nino-article-img"><h3 class="nino-article-title">Design</h3><div class="nino-article-descr">The requirements become a design that works in practice.</div><a href="#">Learn more</a></article>
+			<article class="nino-article nino-article--alt nino-mb-3"><img src="[[/nino/public]]/images/.demo/demo-02.svg" alt="" class="nino-article-img nino-article-img--maxheight"><h3 class="nino-article-title">Design <span class="nino-badge">From 2.400 &euro;</span></h3><div class="nino-article-descr">The requirements become a design that works in practice.</div><a href="#">Learn more</a></article>
 		</div>
 		<div class="nino-grid-33">
-			<article class="nino-article nino-article--alt nino-mb-3"><img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 480 140'%3E%3Crect width='480' height='140' fill='%23bcc0c6'/%3E%3Cpath d='M0 102h480v38H0z' fill='%23a4a9b0'/%3E%3Ccircle cx='126' cy='58' r='32' fill='%23d2d6db'/%3E%3C/svg%3E" alt="" class="nino-article-img"><h3 class="nino-article-title">Implementation</h3><div class="nino-article-descr">Clean markup, clear structure, maintainable for years.</div><a href="#">Learn more</a></article>
+			<article class="nino-article nino-article--alt nino-mb-3"><img src="[[/nino/public]]/images/.demo/demo-00.svg" alt="" class="nino-article-img nino-article-img--maxheight"><h3 class="nino-article-title">Implementation</h3><div class="nino-article-descr">Clean markup, clear structure, maintainable for years.</div><a href="#">Learn more</a></article>
 		</div>
 	</div>
 </section>
 
-<section class="nino-section nino-section--dark nino-pt-3 nino-pb-3 nino-mt-0 nino-mb-0" aria-labelledby="preview-split-title">
+<section class="nino-section nino-section--dark nino-mt-0 nino-mb-0" aria-labelledby="preview-split-title">
 	<div class="nino-grid-row">
-		<div class="nino-grid-50"><div class="nino-img-cover"><img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 640 320'%3E%3Crect width='640' height='320' fill='%23aeb3b9'/%3E%3Ccircle cx='492' cy='96' r='48' fill='%23c9cdd3'/%3E%3Cpath d='M0 380 196 148 372 380z' fill='%23979da5'/%3E%3Cpath d='M296 380 448 206 640 380z' fill='%238b9199'/%3E%3C/svg%3E" alt=""></div></div>
+		<div class="nino-grid-50"><div class="nino-img-cover"><img src="[[/nino/public]]/images/.demo/demo-01.svg" alt=""></div></div>
 		<div class="nino-grid-50">
 			<div class="nino-article">
 				<div class="nino-article-content">
 					<h3 class="nino-article-title" id="preview-split-title">Always included</h3>
 					<p class="nino-article-subtitle">At no extra cost, in every package.</p>
-					<p class="nino-article-description">
-						Body copy sits at the one size the scale never moves. Everything above it fans out from here &mdash; and <a href="#">a link like this one</a> is solved against this exact ground, not against the page it was designed on.
+					<p class="nino-article-descr">
+						Body copy sits at the one size the scale never moves, and <a href="#">a link like this one</a> is solved against this exact ground rather than against the page it was designed on.
 					</p>
-					<div class="nino-pt-4">
+					<div class="nino-pt-2">
 						<span class="nino-alert nino-alert--info">Info</span>
 						<span class="nino-alert nino-alert--success">Success</span>
 						<span class="nino-alert nino-alert--warning">Warning</span>
@@ -129,26 +100,3 @@
 
 </main>
 
-<footer>
-	<div class="nino-footer-main">
-		<div class="nino-grid-row">
-			<div class="nino-grid-100 nino-grid-m-50">
-				<div class="nino-footer-title">Studio Nord</div>
-				<p>Solid wood from the region, delivered throughout Bavaria. <a href="#">One link</a> to measure the deepest ground by.</p>
-			</div>
-			<div class="nino-grid-50 nino-grid-m-25">
-				<div class="nino-footer-title">Company</div>
-				<nav class="nino-nav-content"><ul><li><a href="#">Work</a></li><li><a href="#">Studio</a></li><li><a href="#">Journal</a></li></ul></nav>
-			</div>
-			<div class="nino-grid-50 nino-grid-m-25">
-				<div class="nino-footer-title">Legal</div>
-				<nav class="nino-nav-content"><ul><li><a href="#">Imprint</a></li><li><a href="#">Privacy</a></li><li><a href="#">Terms</a></li></ul></nav>
-			</div>
-		</div>
-	</div>
-	<div class="nino-footer-legal">
-		<div class="nino-grid-row">
-			<div class="nino-grid-100">&copy; Studio Nord &mdash; the deepest ground, which is where most themes put their footer.</div>
-		</div>
-	</div>
-</footer>
diff --git a/_install/Install.php b/_install/Install.php
index 233c811..d4e9d97 100644
--- a/_install/Install.php
+++ b/_install/Install.php
@@ -949,10 +949,9 @@ namespace Nino\Install {
 				return;
 			}
 
-			$unitDir = dirname( self::LIBRARY ). '/'. $kind. '/'. $frame;
-			$source 	= @file_get_contents( $unitDir. '/template.tpl' );
+			$unit = self::frameUnit( $kind, $frame );
 
-			if( $source === false ) {
+			if( $unit === null ) {
 				\Nino\Http::fail( $request, 500, 'could not read the frame template' );
 				return;
 			}
@@ -960,12 +959,78 @@ namespace Nino\Install {
 			\Nino\Http::ok( $request, [
 				'frame'	=> $frame,
 				'html'	=> self::_framePreviewDocument(
-					self::_framePreviewMarkup( $source ),
-					self::_framePreviewCss( $appData, $data, $unitDir )
+					$unit['markup'],
+					self::_framePreviewCss( $appData, $data, dirname( self::LIBRARY ). '/'. $kind. '/'. $frame )
 				),
 			] );
 		}
 
+		/**
+		 *	One frame unit, prepared for a preview: its markup with the
+		 *	includes, fills and shortcodes a request would have resolved, and
+		 *	the stylesheet it ships.
+		 *
+		 *	Public because /_design's Design pane composes its example page out
+		 *	of the same pieces. Its preview used to draw a bar and a footer of
+		 *	its own out of framework classes, which is a bar and a footer no
+		 *	site has - and on a look whose whole idea is a vertical rail it was
+		 *	not a simplification but a wrong answer.
+		 *
+		 *	@param		string		$kind					'header' or 'footer'
+		 *	@param		string		$frame				A unit key
+		 *
+		 *	@return 	?array								{ frame, markup, css }, or null
+		 */
+		public static function frameUnit( string $kind, string $frame ): ?array {
+
+			if( isset( self::FRAMES[$kind] ) === false || in_array( $frame, self::_frames()[$kind] ?? [], true ) === false )
+				return null;
+
+			$unitDir	= dirname( self::LIBRARY ). '/'. $kind. '/'. $frame;
+			$source		= @file_get_contents( $unitDir. '/template.tpl' );
+
+			if( $source === false )
+				return null;
+
+			return [
+				'frame'		=> $frame,
+				'markup'	=> self::_framePreviewMarkup( $source ),
+				'css'		=> is_file( $unitDir. '/style.css' ) === true ? (string) file_get_contents( $unitDir. '/style.css' ) : '',
+			];
+		}
+
+		/**
+		 *	Which frame a preview of this project should show.
+		 *
+		 *	The installed one, or - before the frame steps have run, which is
+		 *	where the installer's Design step sits - the one the active theme
+		 *	declares. A theme names the pair it was drawn against, so that is a
+		 *	better answer than the first unit in the directory, and the first
+		 *	unit is only there so a preview is never frameless.
+		 *
+		 *	@param		array 		&$appData			(reference) Array with current app data
+		 *	@param		string		$kind					'header' or 'footer'
+		 *
+		 *	@return 	?string
+		 */
+		public static function previewFrameKey( array &$appData, string $kind ): ?string {
+
+			$available = self::_frames()[$kind] ?? [];
+
+			if( $available === [] )
+				return null;
+
+			$installed = (string) ( $appData['/nino/install/'. $kind] ?? '' );
+
+			if( in_array( $installed, $available, true ) === true )
+				return $installed;
+
+			$manifest = self::_readManifest( (string) ( $appData['/nino/install/theme'] ?? '' ) );
+			$declared = (string) ( $manifest[$kind] ?? '' );
+
+			return in_array( $declared, $available, true ) === true ? $declared : $available[0];
+		}
+
 		/**
 		 *	Install the one frame selected in its dedicated wizard step. Theme
 		 *	and Design are deliberately untouched: both were committed before
diff --git a/_install/assets/design.js b/_install/assets/design.js
index 923e788..e71e33a 100644
--- a/_install/assets/design.js
+++ b/_install/assets/design.js
@@ -44,7 +44,16 @@
 		// never reaches _settings and never reaches the config
 		_mode 		: 'light',
 		_rowsBound	: false,
+		/*	The layout the example is rendered at, and then scaled into the
+			panel. A desktop's width, because a page never meets its own
+			layout limits in a box half a pane wide - and a height to go with
+			it, so the whole page is on screen rather than the first screen of
+			it. A page with a header, cards carrying photographs, a split band
+			and a real footer is about this tall at this width; giving it the
+			panel's height instead would cut it off, and the cover's own vh
+			lengths would change with the operator's window.	*/
 		PREVIEW_WIDTH : 2400,
+		PREVIEW_HEIGHT : 2200,
 		_refit : null,
 		_example 	: '',
 		_bound 		: false,
@@ -578,7 +587,7 @@
 			const port = dc.getElementById('themes-design-example-port');
 
 			if( frame !== null && port !== null )
-				Nino.install.design._refit = Nino.adminUi.scaleFrame( frame, port, Nino.install.design.PREVIEW_WIDTH );
+				Nino.install.design._refit = Nino.adminUi.scaleFrame( frame, port, Nino.install.design.PREVIEW_WIDTH, Nino.install.design.PREVIEW_HEIGHT );
 
 			Nino.adminUi.buttonRow( {
 				light : dc.getElementById('themes-design-mode-light'),
diff --git a/_install/library/footer/v1/template.tpl b/_install/library/footer/v1/template.tpl
index 1b75578..870cb27 100644
--- a/_install/library/footer/v1/template.tpl
+++ b/_install/library/footer/v1/template.tpl
@@ -1,14 +1,16 @@
 		<footer>
-      <section class="nino-footer-legal nino-grid-row">
-	      <div class="nino-grid-100 nino-grid-l-25">
-	        <p>&copy; [[/date/year]] [[/company/name]]</p>
-	      </div>
-	      <div class="nino-grid-100 nino-grid-l-50 nino-footer-nav">
-	      	[template /templates/html-footer-nav]
-	      	[template /templates/html-footer-legal]
-	      </div>
-	      <div class="nino-grid-100 nino-grid-l-25">
-	        [template /templates/html-footer-localepicker]
-	      </div>
+      <section class="nino-footer-legal">
+      	<div class="nino-grid-row">
+		      <div class="nino-grid-100 nino-grid-l-25">
+		        <p>&copy; [[/date/year]] [[/company/name]]</p>
+		      </div>
+		      <div class="nino-grid-100 nino-grid-l-50 nino-footer-nav">
+		      	[template /templates/html-footer-nav]
+		      	[template /templates/html-footer-legal]
+		      </div>
+		      <div class="nino-grid-100 nino-grid-l-25">
+		        [template /templates/html-footer-localepicker]
+		      </div>
+				</div>
 			</section>
 		</footer>
\ No newline at end of file
diff --git a/_install/library/footer/v2/template.tpl b/_install/library/footer/v2/template.tpl
index 0a93a4e..e36aeee 100644
--- a/_install/library/footer/v2/template.tpl
+++ b/_install/library/footer/v2/template.tpl
@@ -1,41 +1,45 @@
 		<footer>
-			<section class="nino-footer-main nino-grid-row nino-pt-2">
-	      <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
-					<img src="[[/nino/public]]/images/logo-invert.png" class="nino-footer-logo nino-mb-2" alt="[[/company/name]]">
-					<p>[[/company/description]]</p>
-	      </div>
-        <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
-        	<h6 class="nino-footer-title">[[/website/footer/title/followus]]</h6>
-        	[template /templates/html-socialmedia]
-        </div>
-        <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
-        	<h6 class="nino-footer-title">[[/website/footer/title/getintouch]]</h6>
-					<ul class="nino-footer-getintouch">
-						<li>
-							<svg class="nino-icon nino-icon--small" xmlns="http://www.w3.org/2000/svg" viewBox="0 -960 960 960"><path d="M480.09-490q28.91 0 49.41-20.59 20.5-20.59 20.5-49.5t-20.59-49.41q-20.59-20.5-49.5-20.5t-49.41 20.59q-20.5 20.59-20.5 49.5t20.59 49.41q20.59 20.5 49.5 20.5ZM480-159q133-121 196.5-219.5T740-552q0-117.79-75.29-192.9Q589.42-820 480-820t-184.71 75.1Q220-669.79 220-552q0 75 65 173.5T480-159Zm0 79Q319-217 239.5-334.5T160-552q0-150 96.5-239T480-880q127 0 223.5 89T800-552q0 100-79.5 217.5T480-80Zm0-480Z"/></svg>
-							<div>[[/company/adress]], [[/company/country]]</div>
-						</li>
-						<li>
-							<svg class="nino-icon nino-icon--small" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M16 1H8C6.34 1 5 2.34 5 4v16c0 1.66 1.34 3 3 3h8c1.66 0 3-1.34 3-3V4c0-1.66-1.34-3-3-3m-2 20h-4v-1h4zm3.25-3H6.75V4h10.5z"></path></svg>
-							<div>[[/company/phone]]</div>
-						</li>
-						<li>
-							<svg class="nino-icon nino-icon--small" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2m0 14H4V8l8 5 8-5zm-8-7L4 6h16z"></path></svg>
-							<div>[[/company/email]]</div>
-						</li>
-					</ul>
-        </div>
-        <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
-        	[template /templates/html-footer-nav]
-        </div>
+			<section class="nino-footer-main">
+				<div class="nino-grid-row nino-pt-2">
+		      <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
+						<img src="[[/nino/public]]/images/logo-invert.png" class="nino-footer-logo nino-mb-2" alt="[[/company/name]]">
+						<p>[[/company/description]]</p>
+		      </div>
+	        <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
+	        	<h6 class="nino-footer-title">[[/website/footer/title/followus]]</h6>
+	        	[template /templates/html-socialmedia]
+	        </div>
+	        <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
+	        	<h6 class="nino-footer-title">[[/website/footer/title/getintouch]]</h6>
+						<ul class="nino-footer-getintouch">
+							<li>
+								<svg class="nino-icon nino-icon--small" xmlns="http://www.w3.org/2000/svg" viewBox="0 -960 960 960"><path d="M480.09-490q28.91 0 49.41-20.59 20.5-20.59 20.5-49.5t-20.59-49.41q-20.59-20.5-49.5-20.5t-49.41 20.59q-20.5 20.59-20.5 49.5t20.59 49.41q20.59 20.5 49.5 20.5ZM480-159q133-121 196.5-219.5T740-552q0-117.79-75.29-192.9Q589.42-820 480-820t-184.71 75.1Q220-669.79 220-552q0 75 65 173.5T480-159Zm0 79Q319-217 239.5-334.5T160-552q0-150 96.5-239T480-880q127 0 223.5 89T800-552q0 100-79.5 217.5T480-80Zm0-480Z"/></svg>
+								<div>[[/company/adress]], [[/company/country]]</div>
+							</li>
+							<li>
+								<svg class="nino-icon nino-icon--small" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M16 1H8C6.34 1 5 2.34 5 4v16c0 1.66 1.34 3 3 3h8c1.66 0 3-1.34 3-3V4c0-1.66-1.34-3-3-3m-2 20h-4v-1h4zm3.25-3H6.75V4h10.5z"></path></svg>
+								<div>[[/company/phone]]</div>
+							</li>
+							<li>
+								<svg class="nino-icon nino-icon--small" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2m0 14H4V8l8 5 8-5zm-8-7L4 6h16z"></path></svg>
+								<div>[[/company/email]]</div>
+							</li>
+						</ul>
+	        </div>
+	        <div class="nino-grid-100 nino-grid-l-25 nino-pb-2">
+	        	[template /templates/html-footer-nav]
+	        </div>
+				</div>
       </section>
-      <section class="nino-footer-legal nino-grid-row">
-	      <div class="nino-grid-100 nino-grid-l-50 nino-pb-1">
-	        <p>&copy; [[/date/year]] [[/company/name]]</p>
-	      </div>
-	      <div class="nino-grid-100 nino-grid-l-50 nino-pb-1">
-	      	[template /templates/html-footer-legal]
-	        [template /templates/html-footer-localepicker]
-	      </div>
+      <section class="nino-footer-legal">
+      	<div class="nino-grid-row">
+		      <div class="nino-grid-100 nino-grid-l-50 nino-pb-1">
+		        <p>&copy; [[/date/year]] [[/company/name]]</p>
+		      </div>
+		      <div class="nino-grid-100 nino-grid-l-50 nino-pb-1">
+		      	[template /templates/html-footer-legal]
+		        [template /templates/html-footer-localepicker]
+		      </div>
+				</div>
 			</section>
 		</footer>
\ No newline at end of file
diff --git a/_install/library/footer/v3/template.tpl b/_install/library/footer/v3/template.tpl
index 5e1fab2..aa7fe89 100644
--- a/_install/library/footer/v3/template.tpl
+++ b/_install/library/footer/v3/template.tpl
@@ -1,24 +1,28 @@
 		<footer class="nino-frame-footer">
-			<section class="nino-footer-main nino-grid-row">
-				<div class="nino-grid-100 nino-grid-l-50 nino-frame-footer-brand">
-					<img src="[[/nino/public]]/images/logo-invert.png" class="nino-footer-logo" alt="[[/company/name]]">
-					<p>[[/company/description]]</p>
+			<section class="nino-footer-main">
+				<div class="nino-grid-row">
+					<div class="nino-grid-100 nino-grid-l-50 nino-frame-footer-brand">
+						<img src="[[/nino/public]]/images/logo-invert.png" class="nino-footer-logo" alt="[[/company/name]]">
+						<p>[[/company/description]]</p>
+					</div>
+					<address class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-contact">
+						<h2 class="nino-footer-title">[[/website/footer/title/getintouch]]</h2>
+						<p><strong>[[/global/adress]]</strong><br>[[/company/adress]]<br>[[/company/country]]</p>
+						<p><strong>[[/global/phone]]</strong><br>[[/company/phone]]</p>
+						<p><strong>[[/global/email]]</strong><br>[[/company/email]]</p>
+					</address>
+					<nav class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-nav">
+						[template /templates/html-footer-nav]
+					</nav>
 				</div>
-				<address class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-contact">
-					<h2 class="nino-footer-title">[[/website/footer/title/getintouch]]</h2>
-					<p><strong>[[/global/adress]]</strong><br>[[/company/adress]]<br>[[/company/country]]</p>
-					<p><strong>[[/global/phone]]</strong><br>[[/company/phone]]</p>
-					<p><strong>[[/global/email]]</strong><br>[[/company/email]]</p>
-				</address>
-				<nav class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-nav">
-					[template /templates/html-footer-nav]
-				</nav>
 			</section>
-			<section class="nino-footer-legal nino-grid-row">
-				<p class="nino-grid-100 nino-grid-m-50">&copy; [[/date/year]] [[/company/name]]</p>
-				<div class="nino-grid-100 nino-grid-m-50 nino-frame-footer-utility">
-					[template /templates/html-footer-legal]
-					[template /templates/html-footer-localepicker]
+			<section class="nino-footer-legal">
+				<div class="nino-grid-row">
+					<p class="nino-grid-100 nino-grid-m-50">&copy; [[/date/year]] [[/company/name]]</p>
+					<div class="nino-grid-100 nino-grid-m-50 nino-frame-footer-utility">
+						[template /templates/html-footer-legal]
+						[template /templates/html-footer-localepicker]
+					</div>
 				</div>
 			</section>
 		</footer>
\ No newline at end of file
diff --git a/_install/library/footer/v6/template.tpl b/_install/library/footer/v6/template.tpl
index 7a1d73a..ed6df7d 100644
--- a/_install/library/footer/v6/template.tpl
+++ b/_install/library/footer/v6/template.tpl
@@ -1,22 +1,26 @@
 		<footer class="nino-frame-footer">
-			<section class="nino-grid-row nino-frame-footer-content">
-				<div class="nino-grid-100 nino-grid-l-50">
-					<img src="[[/nino/public]]/images/logo.png" class="nino-footer-logo" alt="[[/company/name]]">
+			<section class="nino-frame-footer-content">
+				<div class="nino-grid-row">
+					<div class="nino-grid-100 nino-grid-l-50">
+						<img src="[[/nino/public]]/images/logo.png" class="nino-footer-logo" alt="[[/company/name]]">
+					</div>
+					<address class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-contact">
+						<h3>[[/website/footer/title/getintouch]]</h3>
+						<p>[[/company/adress]]<br>[[/company/country]]</p>
+						<p>[[/company/phone]]<br>[[/company/email]]</p>
+					</address>
+					<nav class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-nav">
+						[template /templates/html-footer-nav]
+					</nav>
 				</div>
-				<address class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-contact">
-					<h3>[[/website/footer/title/getintouch]]</h3>
-					<p>[[/company/adress]]<br>[[/company/country]]</p>
-					<p>[[/company/phone]]<br>[[/company/email]]</p>
-				</address>
-				<nav class="nino-grid-100 nino-grid-m-50 nino-grid-l-25 nino-frame-footer-nav">
-					[template /templates/html-footer-nav]
-				</nav>
 			</section>
-			<section class="nino-grid-row nino-frame-footer-legal">
-				<p>&copy; [[/date/year]] [[/company/name]]</p>
-				<div class="nino-frame-footer-utility">
-					[template /templates/html-footer-legal]
-					[template /templates/html-footer-localepicker]
+			<section class="nino-frame-footer-legal">
+				<div class="nino-grid-row">
+					<p>&copy; [[/date/year]] [[/company/name]]</p>
+					<div class="nino-frame-footer-utility">
+						[template /templates/html-footer-legal]
+						[template /templates/html-footer-localepicker]
+					</div>
 				</div>
 			</section>
 		</footer>
\ No newline at end of file
diff --git a/_install/library/header/v1/style.css b/_install/library/header/v1/style.css
index 4dbe423..3a2bb97 100644
--- a/_install/library/header/v1/style.css
+++ b/_install/library/header/v1/style.css
@@ -17,7 +17,7 @@ body.nino-scroll-atf .nino-frame-header .nino-scroll-header {
 	height: auto;
 	min-width: 0;
 	min-height: 0;
-	margin: 0;
+	margin: var(--space-2) 0 0 0;
 	background-image: none;
 }
 
diff --git a/_install/library/header/v1/template.tpl b/_install/library/header/v1/template.tpl
index 0306c90..0611a5d 100644
--- a/_install/library/header/v1/template.tpl
+++ b/_install/library/header/v1/template.tpl
@@ -1,13 +1,15 @@
 		<header class="nino-frame-header">
-			<div class="nino-grid-row nino-grid-middle nino-scroll-header">
-				<div class="nino-grid-100">
-					<a href="[[/nino/dir]]/" class="nino-logo">
-						<img src="[[/nino/public]]/images/logo.png" alt="">
-						<h1 class="nino-sr-only" id="header-title">[[/company/name]] | [[/webpage[[/nino/http/response/uri]]/title]]</h1>
-					</a>
-					<nav class="nino-frame-header-nav">
-						[template /templates/html-header-nav]
-					</nav>
+			<div class="nino-scroll-header">
+				<div class="nino-grid-row nino-grid-middle">
+					<div class="nino-grid-100">
+						<a href="[[/nino/dir]]/" class="nino-logo">
+							<img src="[[/nino/public]]/images/logo.png" alt="">
+							<h1 class="nino-sr-only" id="header-title">[[/company/name]] | [[/webpage[[/nino/http/response/uri]]/title]]</h1>
+						</a>
+						<nav class="nino-frame-header-nav">
+							[template /templates/html-header-nav]
+						</nav>
+					</div>
 				</div>
 			</div>
 		</header>
\ No newline at end of file
diff --git a/_install/library/header/v3/template.tpl b/_install/library/header/v3/template.tpl
index 50ef59f..22ee863 100644
--- a/_install/library/header/v3/template.tpl
+++ b/_install/library/header/v3/template.tpl
@@ -1,13 +1,15 @@
 		<header class="nino-frame-header">
-			<div class="nino-grid-row nino-grid-row--wide nino-grid-middle nino-scroll-header">
-				<div class="nino-grid-100">
-					<a href="[[/nino/dir]]/" class="nino-logo">
-						<img src="[[/nino/public]]/images/logo.png" alt="">
-						<h1 class="nino-sr-only" id="header-title">[[/company/name]] | [[/webpage[[/nino/http/response/uri]]/title]]</h1>
-					</a>
-					<nav class="nino-frame-header-nav">
-						[template /templates/html-header-nav]
-					</nav>
+			<div class="nino-scroll-header">
+				<div class="nino-grid-row nino-grid-row--wide nino-grid-middle">
+					<div class="nino-grid-100">
+						<a href="[[/nino/dir]]/" class="nino-logo">
+							<img src="[[/nino/public]]/images/logo.png" alt="">
+							<h1 class="nino-sr-only" id="header-title">[[/company/name]] | [[/webpage[[/nino/http/response/uri]]/title]]</h1>
+						</a>
+						<nav class="nino-frame-header-nav">
+							[template /templates/html-header-nav]
+						</nav>
+					</div>
 				</div>
 			</div>
 		</header>
\ No newline at end of file
diff --git a/_install/library/header/v4/template.tpl b/_install/library/header/v4/template.tpl
index d8dbed8..41b0970 100644
--- a/_install/library/header/v4/template.tpl
+++ b/_install/library/header/v4/template.tpl
@@ -1,13 +1,15 @@
 		<header class="nino-frame-header">
-			<div class="nino-grid-row nino-grid-middle nino-scroll-header">
-				<div class="nino-grid-100 nino-frame-header-inner">
-					<a href="[[/nino/dir]]/" class="nino-logo nino-frame-header-brand">
-						<img src="[[/nino/public]]/images/logo-invert.png" alt="">
-						<h1 class="nino-sr-only" id="header-title">[[/company/name]] | [[/webpage[[/nino/http/response/uri]]/title]]</h1>
-					</a>
-					<nav class="nino-frame-header-nav">
-						[template /templates/html-header-nav]
-					</nav>
+			<div class="nino-scroll-header">
+				<div class="nino-grid-row nino-grid-middle">
+					<div class="nino-grid-100 nino-frame-header-inner">
+						<a href="[[/nino/dir]]/" class="nino-logo nino-frame-header-brand">
+							<img src="[[/nino/public]]/images/logo-invert.png" alt="">
+							<h1 class="nino-sr-only" id="header-title">[[/company/name]] | [[/webpage[[/nino/http/response/uri]]/title]]</h1>
+						</a>
+						<nav class="nino-frame-header-nav">
+							[template /templates/html-header-nav]
+						</nav>
+					</div>
 				</div>
 			</div>
 		</header>
\ No newline at end of file
diff --git a/_nino/Nino.admin.js b/_nino/Nino.admin.js
index 46554d8..6e532e7 100644
--- a/_nino/Nino.admin.js
+++ b/_nino/Nino.admin.js
@@ -83,10 +83,13 @@
 		 *	@param	{Element}		frame			The iframe, absolutely placed in its port
 		 *	@param	{Element}		port			The box it is shown inside
 		 *	@param	{number}		width			The layout width to render at
+		 *	@param	{number}		[height]	The layout height to render at. Given, the
+		 *														whole page is fitted into the port; omitted, the
+		 *														page is given as much height as the port shows
 		 *
 		 *	@return	{Function}						fit(), for calling after a layout change
 		 */
-		scaleFrame : function( frame, port, width ) {
+		scaleFrame : function( frame, port, width, height ) {
 
 			const fit = function() {
 
@@ -95,13 +98,33 @@
 				if( room.width < 1 || room.height < 1 )
 					return;
 
+				/*	Without a height the page is given the panel's, solved back
+					through the scale: as much document as the port can show and
+					no more. That fits by width alone, so a page taller than the
+					port scrolls - and a page whose own lengths are viewport
+					relative gets a different viewport on every window size.
+
+					With one, both dimensions are fixed and the scale is
+					whichever of the two fits: the whole page is on screen at a
+					stable layout, and what changes with the panel is how small
+					it is drawn rather than how much of it there is.	*/
+				const solved = height > 0 ? height : Math.round( room.height / Math.min( 1, room.width / width ) );
+
 				// Never magnified: on a panel wider than a desktop browser the page
 				// should be shown at its own size, not blown up past it
-				const factor = Math.min( 1, room.width / width );
+				const factor = Math.min( 1, room.width / width, room.height / solved );
 
 				frame.style.width = width+ 'px';
-				frame.style.height = Math.round( room.height / factor )+ 'px';
+				frame.style.height = solved+ 'px';
 				frame.style.transform = 'scale('+ factor+ ')';
+
+				/*	A page is taller than it is wide and a preview panel is the
+					other way round, so fitting the whole page leaves room at the
+					sides. Centred rather than left in the corner: the empty half
+					of the port then reads as a margin instead of as a page that
+					failed to fill it.	*/
+				frame.style.left = height > 0 ? Math.round( ( room.width - width * factor ) / 2 )+ 'px' : '0';
+				frame.style.top = height > 0 ? Math.round( ( room.height - solved * factor ) / 2 )+ 'px' : '0';
 			};
 
 			fit();
diff --git a/tests/design-js-smoke.js b/tests/design-js-smoke.js
index 3d8d311..757a3dc 100644
--- a/tests/design-js-smoke.js
+++ b/tests/design-js-smoke.js
@@ -271,11 +271,17 @@ check( 'Design opens clean, with nothing to save and nothing to revert', nodes['
 	frame was narrower than the difference the operator was choosing between.
 	The page is given a desktop's layout width and scaled into the panel
 	instead, which keeps every proportion inside it exact.	*/
-check( 'the page renders at a desktop layout width, not at the panel\'s', nodes['theme-design-example'].style.width === '1600px' );
-// At scale f, filling a box h tall takes h/f of document - so the page gets
-// exactly as much vertical room as the panel can show, and no more
-check( '...scaled into the room the panel has, height solved to match', nodes['theme-design-example'].style.transform === 'scale(0.5)'
-	&& nodes['theme-design-example'].style.height === '1200px' );
+check( 'the page renders at a desktop layout, not at the panel\'s', nodes['theme-design-example'].style.width === String( theme.PREVIEW_WIDTH )+ 'px'
+	&& nodes['theme-design-example'].style.height === String( theme.PREVIEW_HEIGHT )+ 'px' );
+/*	Both dimensions are fixed, so the scale is whichever of the two fits - the
+	whole page is on screen, and what changes with the panel is how small it is
+	drawn rather than how much of it there is. The stub's port is 800x600.	*/
+check( '...and scaled by whichever dimension fits, so the whole page is on screen',
+	nodes['theme-design-example'].style.transform === 'scale('+ Math.min( 1, 800 / theme.PREVIEW_WIDTH, 600 / theme.PREVIEW_HEIGHT )+ ')' );
+// A page is taller than it is wide and a panel is the other way round, so the
+// room left over reads as a margin rather than as a page that failed to fill it
+check( '...centred in the room that leaves', nodes['theme-design-example'].style.left !== '0'
+	&& nodes['theme-design-example'].style.top !== undefined );
 
 // Two tabs rather than one long column. Both panels stay in the DOM - the one
 // not being looked at keeps its controls, their values and their listeners
diff --git a/tests/design-smoke.php b/tests/design-smoke.php
index 55c6614..a5bba7f 100644
--- a/tests/design-smoke.php
+++ b/tests/design-smoke.php
@@ -688,10 +688,14 @@ check( 'no unresolved fill is left in a value', str_contains( $example, '[[' ) =
 	does carry - the padding that keeps a fixed bar off the first heading, and
 	the row its nav links sit in - come from the scaffold, so no length in the
 	markup is one this preview invented.	*/
-foreach( [ '<header>', '<footer>', 'nino-atf-title', 'nino-article--grid', 'nino-section--alt', 'nino-footer-main', 'nino-footer-legal' ] as $part )
+foreach( [ 'nino-atf-title', 'nino-article', 'nino-section--dark', 'nino-alert--error' ] as $part )
 	check( 'the example is a page, not a specimen strip: '. $part, str_contains( $example, $part ) );
 
-check( 'the scaffold supplies what a frame would, rather than the markup inventing it', str_contains( $example, 'body main{padding-top:' )
+/*	A delivery may ship without /_install, and then there are no frame units to
+	have. The example stands on its own, and the two rules a header unit would
+	have brought - the padding that keeps a fixed bar off the first heading, and
+	the row its nav links sit in - come from the scaffold instead.	*/
+check( 'without /_install the example stands on its own, and the scaffold stands in', str_contains( $example, 'body main{padding-top:' )
 	&& str_contains( $example, 'header .nino-nav-content ul{display:flex' ) );
 
 // A srcdoc frame fetches nothing, so the photographs travel as data - and a
diff --git a/tests/install-design-js-smoke.js b/tests/install-design-js-smoke.js
index 0724f95..5524af5 100644
--- a/tests/install-design-js-smoke.js
+++ b/tests/install-design-js-smoke.js
@@ -206,12 +206,11 @@ check( 'the example is delivered whole, as a document into the sandboxed frame',
 	The page is given a desktop's layout width and scaled into the panel
 	instead, which keeps every proportion inside it exact.	*/
 check( 'the page renders at a desktop layout width, not at the panel\'s', nodes['themes-design-example'].style.width === String( design.PREVIEW_WIDTH )+ 'px' );
-// At scale f, filling a box h tall takes h/f of document - so the page gets
-// exactly as much vertical room as the panel can show, and no more
-// The port is 800x600 in this stub, so the factor is 800/width and the page
-// gets 600/factor of document - exactly as much as the panel can show
-check( '...scaled into the room the panel has, height solved to match', nodes['themes-design-example'].style.transform === 'scale('+ ( 800 / design.PREVIEW_WIDTH )+ ')'
-	&& nodes['themes-design-example'].style.height === String( Math.round( 600 / ( 800 / design.PREVIEW_WIDTH ) ) )+ 'px' );
+/*	Both dimensions are fixed, so the scale is whichever of the two fits and the
+	whole page is on screen - what changes with the panel is how small it is
+	drawn rather than how much of it there is. The stub's port is 800x600.	*/
+check( '...and scaled by whichever dimension fits, so the whole page is on screen', nodes['themes-design-example'].style.height === String( design.PREVIEW_HEIGHT )+ 'px'
+	&& nodes['themes-design-example'].style.transform === 'scale('+ Math.min( 1, 800 / design.PREVIEW_WIDTH, 600 / design.PREVIEW_HEIGHT )+ ')' );
 
 // Two tabs rather than one long column, the same pair /_design has
 check( 'the step opens on Colour, with Raster present but hidden', nodes['themes-design-panel-colour'].hidden === false
diff --git a/tests/install-smoke.php b/tests/install-smoke.php
index 077015c..e8fe139 100644
--- a/tests/install-smoke.php
+++ b/tests/install-smoke.php
@@ -642,6 +642,19 @@ check( 'the picker can ask what a setting produces without storing it', str_star
 	&& str_contains( $previewLight, '--nino-brand: #4faae8;' )
 	&& str_contains( $previewLight, 'data-nino-mode="light"' ) );
 
+/*	...inside the frames this project will actually have. Before the Header and
+	Footer steps have run there is nothing installed to read, so the theme's own
+	declaration decides - a theme names the pair it was drawn against, and a
+	Design judged inside somebody else's bar is a Design judged wrong. On a look
+	built around a vertical rail it was not a detail but the whole layout.	*/
+check( '...inside the project\'s own header and footer, not a bar invented for the preview', str_contains( $previewLight, 'nino-frame-header' )
+	&& str_contains( $previewLight, '<footer' )
+	&& str_contains( $previewLight, 'nino-nav-content' ) );
+// A srcdoc frame fetches nothing, so whatever the example points at travels
+// with it - and the fills a request would have resolved are resolved here too
+check( '...with the pictures carried into it and no fill left showing', str_contains( $previewLight, 'src="data:image/' )
+	&& str_contains( $previewLight, '[[' ) === false );
+
 // The previewed mode travels beside the settings, never inside them: a
 // sandboxed iframe has an opaque origin, so nothing in the browser can stamp
 // the attribute the generated stylesheet keys its dark block on
-- 
2.17.1

