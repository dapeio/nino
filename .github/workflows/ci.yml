name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Lint all PHP files
        run: find . -name "*.php" -not -path "./.git/*" -print0 | xargs -0 -n1 php -l

      - name: Lint all JS files
        run: find . -name "*.js" -not -path "./.git/*" -print0 | xargs -0 -n1 node --check

      - name: Run kernel smoke tests
        run: php tests/kernel-smoke.php

      - name: Run Elements search smoke tests
        run: php tests/search-smoke.php

      - name: Run editor backend smoke tests
        run: php tests/editor-smoke.php

      - name: Run admin tooling smoke tests
        run: php tests/admin-smoke.php

      - name: Run install wizard smoke tests
        run: php tests/install-smoke.php

      - name: Run appearance tooling smoke tests
        run: php tests/theme-smoke.php

      - name: Run template builder smoke tests
        run: php tests/templates-smoke.php

      - name: Run frontend smoke tests
        run: for test in tests/*-js-smoke.js; do node "$test"; done

      - name: Run concurrency smoke tests
        run: php tests/concurrency-smoke.php
